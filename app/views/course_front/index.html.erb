<%= render 'layouts/headers' %>
<body data-no-turbolink="true">
<%= render "layouts/main_menu" %>
<div id="courseContent" ng-app="courseApp" ng-controller="myCtrl" ng-init="courseId='559ce5ca416d690ea0000002'; initCourse()">
    <div id="courseLoading" class="text-center col-md-10 col-md-offset-1 mrg25t" ng-hide="inited">
        <div class="progress progress-striped active">
            <div class="progress-bar" role="progressbar" style="width: 100%;">
                Wczytywanie kursu...
            </div>
        </div>
    </div>
    <div ng-hide="!inited">
        <div id="courseLessonMenu" class="first col" ng-hide="resizeElements(1);!menuVisible.left">
            <ul>
                <li><i class="glyphicon glyphicon-align-justify"></i> <a href="#" ng-click="goToPart('article')">Artykuł</a></li>
                <ul class="article-parsed">
                </ul>
                <li ng-hide="!course.lessons[course.lessonCurrent].quizzes.length"><i class="glyphicon glyphicon-check"></i> <a href="#" ng-click="goToPart('quiz')">Quiz</a></li>
                <li ng-hide="!course.lessons[course.lessonCurrent].exercises.length"><i class="glyphicon glyphicon-console"></i> <a href="#" ng-click="goToPart('exercise')">Zadanie</a></li>
            </ul>
        </div>
        <div class="col col-line-height-100-pro firstHidePanelBar" ng-click="hidePanelBarClick('left')">||</div>
        <div id="courseCnt" class="sec col">
            <div class="course-article course-element" ng-hide="!isPartVisible('article')" ng-bind-html="getLesson().article | to_trusted">
            </div>

            <div class="buttons" ng-hide="!isPartVisible('article')">
                To już wszystko w tej części lekcji <input type="submit" class="btn btn-lg btn-primary btn-save" value="Kliknij tutaj aby przejść dalej" ng-click="goToPart('quiz')">
            </div>

            <div class="course-quiz course-element" ng-hide="!isPartVisible('quiz')">
                <form>
                    <div class="caption">Quiz</div>

                    <div class="form-group" ng-repeat="(key, quiz) in getLesson().quizzes">
                        <div class="field-name">
                            <label for="quiz_question_{{quiz.ID}}"><small>{{ quiz.question }}</small></label>
                        </div>
                        <div class="field-value">
                            <select id="quiz_question_{{quiz.ID}}" selectpicker class="selectpicker" data-width="100%">
                                <option ng-repeat="(ans_idx, ans) in quiz.answers" value="{{ans_idx}}">{{ans}}</option>
                            </select>
                        </div>
                    </div>

                    <div class="clearfix"></div>
                    <div class="buttons">
                        <input type="submit" class="btn btn-lg btn-primary btn-save" value="Sprawdź odpowiedzi">
                    </div>
                </form>
            </div>

            <div class="course-exercise course-element" ng-hide="!isPartVisible('exercise')">
                Jakieś tam zadanie...
            </div>
        </div>
        <div class="clearfix"></div>
        <div class="secHidePanelBar" ng-click="hidePanelBarClick('bottom')">=</div>
        <div id="courseLessons" ng-hide="resizeElements(1);!menuVisible.bottom">
            <div class="pad25a">
                <div class="progress active" style="height:5px">
                    <div class="progress-bar" role="progressbar" style="width: 50%;">
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>